import 'package:flutter/material.dart';

class ProjectsPortrait extends StatefulWidget {
  const ProjectsPortrait({Key? key}) : super(key: key);

  @override
  _ProjectsPortraitState createState() => _ProjectsPortraitState();
}

class _ProjectsPortraitState extends State<ProjectsPortrait> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: SingleChildScrollView(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
